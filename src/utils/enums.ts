export enum Device {
  Mobile = "mobile",
  Desktop = "desktop",
  Tablet = "tablet",
}

export enum ReportAction {
  edit = "Hesabatın redaktəsi",
  create = "Yeni hesabat",
}

export enum Roles {
  user = "USER",
  admin = "ADMIN",
}

export enum FieldsLabel {
  organization = "Qurum_",
  district = "Rayon",
  factUse = "Fact_use",
}

export enum mapTabs {
  info = "Ərazi məlumatları",
  filter = "Filter",
}